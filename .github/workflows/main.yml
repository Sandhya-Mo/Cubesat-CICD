name: Create Release

on:
  push:
    tags:
      - 'v*'  # Triggers on version tags like v1.0.0

jobs:      
  release:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3 #clones repo
      
      - name: Create Release
        uses: softprops/action-gh-release@v1 #creates release
